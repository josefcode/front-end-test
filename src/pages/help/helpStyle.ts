import { styled } from "@mui/material";


export const Root = styled('main')({
        height: '100vh',
        display:'flex',
        justifyContent: 'center',
        alignItems: 'center',

        p:  {
            marginBlock: '2rem'
        }
     
})